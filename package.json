{
  "name": "mockup-studio-workspace",
  "private": true,
  "description": "Local workspace: marketing=Astro site (folder marketing/). Editor=Vite app—use folder editor/ after git mv mockup-generator editor; until then run npm --prefix ./mockup-generator for editor.",
  "scripts": {
    "dev:marketing": "npm --prefix ./marketing run dev",
    "build:marketing": "npm --prefix ./marketing run build",
    "dev:editor": "npm --prefix ./mockup-generator run dev",
    "build:editor": "npm --prefix ./mockup-generator run build",
    "dev:blog": "npm run dev:marketing",
    "dev:studio": "npm run dev:editor",
    "build:blog": "npm run build:marketing"
  }
}
